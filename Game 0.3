Prerequisites:
Python: Ensure Python is installed on the system.
Pygame: Install Pygame by opening the terminal in VS Code and running:
Code

    pip install pygame




import pygame
import random

# Initialize Pygame
pygame.init()

# Screen dimensions
SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600
screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
pygame.display.set_caption("Simple Survival Game")

# Colors
WHITE = (255, 255, 255)
RED = (255, 0, 0)
BLUE = (0, 0, 255)

# Player properties
player_size = 50
player_x = (SCREEN_WIDTH - player_size) // 2
player_y = SCREEN_HEIGHT - player_size - 10
player_speed = 5

# Enemy properties
enemy_size = 30
enemy_speed = 3
enemies = []

# Game loop
running = True
clock = pygame.time.Clock()

def create_enemy():
    enemy_x = random.randint(0, SCREEN_WIDTH - enemy_size)
    enemy_y = random.randint(-SCREEN_HEIGHT, -enemy_size) # Start off-screen
    enemies.append(pygame.Rect(enemy_x, enemy_y, enemy_size, enemy_size))

# Initial enemies
for _ in range(5):
    create_enemy()

while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

    # Player movement
    keys = pygame.key.get_pressed()
    if keys[pygame.K_LEFT] and player_x > 0:
        player_x -= player_speed
    if keys[pygame.K_RIGHT] and player_x < SCREEN_WIDTH - player_size:
        player_x += player_speed

    # Enemy movement and collision
    for enemy in enemies:
        enemy.y += enemy_speed
        if enemy.y > SCREEN_HEIGHT: # Reset enemy if it goes off-screen
            enemy.x = random.randint(0, SCREEN_WIDTH - enemy_size)
            enemy.y = random.randint(-SCREEN_HEIGHT, -enemy_size)
        
        # Collision detection
        player_rect = pygame.Rect(player_x, player_y, player_size, player_size)
        if player_rect.colliderect(enemy):
            print("Game Over!")
            running = False # End game on collision

    # Drawing
    screen.fill(WHITE) # Background
    pygame.draw.rect(screen, BLUE, (player_x, player_y, player_size, player_size)) # Player
    for enemy in enemies:
        pygame.draw.rect(screen, RED, enemy) # Enemies

    pygame.display.flip() # Update the display
    clock.tick(60) # Cap framerate

pygame.quit()


Running the Code in VS Code:
Save the code as main.py in a folder.
Open that folder in VS Code.
Open the integrated terminal in VS Code (Terminal > New Terminal).
Navigate to the directory where main.py is saved if not already there.
Run the script using:
Code

    python main.py
